@model IEnumerable<CapstoneProject.Models.UserLlc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User.Email) // Note: Changed from .UserId to .User.Email
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Llc.LlcName) // Note: Changed from .LlcId to .Llc.LlcName
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email) // Note: Changed from .UserId to .User.Email
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Llc.LlcName) // Note: Changed from .LlcId to .Llc.LlcName
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserLlcId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserLlcId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserLlcId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
